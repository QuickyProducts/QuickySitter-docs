repository: QuickyProducts/QuickySitter-docs

# project-pages: served from https://quickyproducts.github.io/QuickySitter-docs/
baseurl: /QuickySitter-docs

output: web
# this property is useful for conditional filtering of content that is separate from the PDF.

topnav_title: QuickySitter&trade; documentation
# this appears on the top navigation bar next to the home button

site_title: QuickySitter&trade; documentation
# this appears in the html browser tab for the site title (seen mostly by search engines, not users)

company_name: QuickyProducts
# this appears in the footer

github_editme_path: QuickyProducts/QuickySitter-docs/edit/devel/
# if you're using Github, provide the basepath to the branch you've created for reviews, following the sample here. if not, leave this value blank.

disqus_shortname:
# if you're using disqus for comments, add the shortname here. if not, leave this value blank.

google_analytics:
# if you have google-analytics ID, put it in. if not, edit this value to blank.

host: 127.0.0.1
# the preview server used. Leave as is.

port: 4000
# the port where the preview is rendered. You can leave this as is unless you have other Jekyll builds using this same port that might cause conflicts. in that case, use another port such as 4006.

exclude:
  - .idea/
  - .gitignore
  - Dockerfile
  - Gemfile
  - Gemfile.lock
  - update.sh
# these are the files and directories that jekyll will exclude from the build

feedback_subject_line: QuickySitter documentation

feedback_email:
# used as a contact email for the Feedback link in the top navigation bar

feedback_disable: false
# if you uncomment the previous line, the Feedback link gets removed

# feedback_text: "Need help?"
# if you uncomment the previous line, it changes the Feedback text

feedback_link: "https://github.com/QuickyProducts/QuickySitter-docs/issues"
# if you uncomment the previous line, it changes where the feedback link points to

highlighter: rouge
# library used for syntax highlighting

markdown: kramdown
kramdown:
 input: GFM
 auto_ids: true
 hard_wrap: false
 syntax_highlighter: rouge

# filter used to process markdown. note that kramdown differs from github-flavored markdown in some subtle ways

collections:
  avsitter1_lsl_examples_beginner:
    output: true
  avsitter1_lsl_examples_advanced:
    output: true
  avsitter2_lsl_examples_beginner:
    output: true
  avsitter2_lsl_examples_advanced:
    output: true
  avsitter2_lsl_examples_avprop:
    output: true
# collections are declared here. this renders the content in _xxxx and processes it, but doesn't output it as actual files in the output unless you change output to true

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
      comments: false
      search: true
      sidebar: home_sidebar
      topnav: topnav
  -
    scope:
      path: ""
      type: "tooltips"
    values:
      layout: "page"
      comments: false
      search: true
      tooltip: true


# these are defaults used for the frontmatter for these file types

sidebars:
- home_sidebar
- avsitter2_sidebar
- avsitter1_sidebar

description: "QuickySitter documentation: a fork of AVsitter 2 with LSD storage, HUD integration, and a SYNC re-sync API."
# the description is used in the feed.xml file

# needed for sitemap.xml file only
url: https://quickyproducts.github.io

# Site variables
script_github: https://github.com/QuickyProducts/QuickySitter
upstream_github: https://github.com/AVsitter/AVsitter
sl_website: https://secondlife.com
marketplace:
inworld:

# Content attribution
# This site re-uses portions of the AVsitter documentation by Avcode Technologies,
# licensed CC-BY-SA 4.0. QuickySitter additions are also CC-BY-SA 4.0.
# Theme is the Documentation Theme for Jekyll by Tom Johnson (MIT).
